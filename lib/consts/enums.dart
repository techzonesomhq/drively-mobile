enum AppLanguages { ar, en }

enum AppGender { male, female }

enum ApiRequestType { get, post, put, delete }

enum LauncherType { link, mobile, email }

enum CacheKeys {
  firstVisit,
  loggedIn,
  remember,
  language,
  theme,
  token,
  fcmToken,
  appSignature,
  name,
  mobile,
  email,
}
